# gelClust - an R package for gamma clustering
# Installation:
First, make sure you have devtools installed and loaded
```
install.packages("devtools")
library(devtools)
```
Then, install this package
```
install_packages("aarkatkar/gelClust")
```
